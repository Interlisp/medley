(DEFINE-FILE-INFO PACKAGE "INTERLISP" READTABLE "INTERLISP" BASE 10)

(FILECREATED "23-Dec-2023 17:59:26" {DSK}<home>briggs>medley>greetfiles>EMSCRIPTEN-INIT.;7 3115   

      :EDIT-BY "briggs"

      :CHANGES-TO (VARS EMSCRIPTEN-INITCOMS)

      :PREVIOUS-DATE "23-Dec-2023 14:06:44" {DSK}<home>briggs>medley>greetfiles>EMSCRIPTEN-INIT.;6)


(PRETTYCOMPRINT EMSCRIPTEN-INITCOMS)

(RPAQQ EMSCRIPTEN-INITCOMS
       ((P (KEYACTION 'LOCK '(LOCKTOGGLE . IGNORE))
           (SETUSERNAME "web_user"))
                                                             (* ; "Emscripten/wasm default")
        (VARS 
              (* ;; "settings for new users")

              (LOGINHOST/DIR "{DSK}<home>web_user>")
              [USERGREETFILES `((,LOGINHOST/DIR "INIT" COM)
                                (,LOGINHOST/DIR "INIT"]
              (FOODIRS '(FOO BAR))
              (LISPUSERSDIRECTORIES '("{DSK}<medley>lispusers>"))
              (DIRECTORIES '("{DSK}<medley>library>" "{DSK}<medley>lispusers>" 
                                   "{DSK}<medley>sources>"))
              (DWIMWAIT 180)
              (HELPDEPTH 4)
              (HELPTIME 1)
              (FILING.ENUMERATION.DEPTH 1)
              (COPYRIGHTFLG 'NEVER)
              (COPYRIGHTSRESERVED NIL)
              (AUTOBACKTRACEFLG 'ALWAYS)
              (MAXLEVEL 30000)
              (MAXLOOP 30000))
        (FILES BACKGROUND-YIELD)
        (FNS INTERLISPMODE)
        (ALISTS (FONTDEFS))))

(KEYACTION 'LOCK '(LOCKTOGGLE . IGNORE))

(SETUSERNAME "web_user")



(* ; "Emscripten/wasm default")


(RPAQ LOGINHOST/DIR "{DSK}<home>web_user>")

(RPAQ USERGREETFILES `((,LOGINHOST/DIR "INIT" COM)
                       (,LOGINHOST/DIR "INIT")))

(RPAQQ FOODIRS (FOO BAR))

(RPAQQ LISPUSERSDIRECTORIES ("{DSK}<medley>lispusers>"))

(RPAQQ DIRECTORIES ("{DSK}<medley>library>" "{DSK}<medley>lispusers>" "{DSK}<medley>sources>"))

(RPAQQ DWIMWAIT 180)

(RPAQQ HELPDEPTH 4)

(RPAQQ HELPTIME 1)

(RPAQQ FILING.ENUMERATION.DEPTH 1)

(RPAQQ COPYRIGHTFLG NEVER)

(RPAQQ COPYRIGHTSRESERVED NIL)

(RPAQQ AUTOBACKTRACEFLG ALWAYS)

(RPAQQ MAXLEVEL 30000)

(RPAQQ MAXLOOP 30000)

(FILESLOAD BACKGROUND-YIELD)
(DEFINEQ

(INTERLISPMODE
  [LAMBDA (OLD?)                                             (* N.H.Briggs " 2-Feb-88 14:26")
    (PROCESSPROP 'EXEC 'PROFILE (XCL:COPY-PROFILE (if OLD?
                                                      then "OLD-INTERLISP-T"
                                                    else "INTERLISP")))
    (XCL:SET-DEFAULT-EXEC-TYPE (if OLD?
                                   then "OLD-INTERLISP-T"
                                 else "INTERLISP"))
    (SETQ *DEFAULT-MAKEFILE-ENVIRONMENT* (LIST :READTABLE (if OLD?
                                                              then "OLD-INTERLISP-FILE"
                                                            else "INTERLISP")
                                               :PACKAGE "INTERLISP"])
)

(ADDTOVAR FONTDEFS )
(DECLARE%: DONTCOPY
  (FILEMAP (NIL (2241 3066 (INTERLISPMODE 2251 . 3064)))))
STOP
