Testing AR  10062:In an Interlisp Exec:LOAD({Eris}<Test>Env>FilePkg>Hand-Aux>FORMATTINGFNS)MAKEFILE({CORE}FORMATTINGFNS)  ;;shouldn't cause an error.DELFILE({CORE}FORMATTINGFNS)The test is successful if the MAKEFILE runs to completion without causing an error.